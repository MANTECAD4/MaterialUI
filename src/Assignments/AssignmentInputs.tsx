import { Grid, Slider, Typography } from "@mui/material";

export const AssignmentInputs = () => {
  return (
    <Grid container direction="column">
      <Typography variant="h3">My Slider</Typography>
      <Slider />
    </Grid>
  );
};
